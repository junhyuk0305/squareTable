# 아키텍처 — 가입/합류 (S1 구조 재구성)

> 메가프롬프트 감사 산출물. SCOPE=가입/합류. 생성 2026-07-06.
> 라이브 baseline: `npm run qa:onboarding` = **19 passed, 0 failed** (프로덕션 실 백엔드, 자가정리).

## S1 계층 그래프

```
[화면]  signup.tsx · login.tsx · junior/hub.tsx(합류·대기) · junior/onboarding.tsx
        owner/onboarding.tsx(가게만들기) · owner/staff.tsx(승인/거절) · account-edit.tsx · billing.tsx
   │
   ├─(직원/본인측)──────────────► [스토어] useSessionStore.ts  ──raw supabase──►┐
   │   signUp·signIn(=auth.* 만 스토어 소유) + createStore·joinByInvite·cancel·  │
   │   leave·delete·updateProfile·updateIndustry·rename·phone·loadProfile 3 read  │
   │   ✅ Finding A 수렴 — 데이터 접근은 db.ts 세션 데이터함수로만(auth 제외)       │
   │                                        └─ db.ts ─┐                            ▼
   └─(사장/승인측)──► [스토어] useStaffStore.ts ──► [데이터] db.ts ──────► [Supabase]
       hydrate·approve·reject(낙관 롤백✓)          fetchStaffProfiles          │
                                                    fetchPendingMembers         │ RLS·RPC·트리거
                                                    approveMember/rejectMember  │
                                                    subscribeStaff(realtime)    ▼
                                                                    ┌───────────────────────────┐
                                                                    │ RPC: create_store(0040)   │
                                                                    │      join_by_invite(0038) │
                                                                    │      approve/reject/cancel(0038)
                                                                    │      phone_in_use·delete_my_account(0044)
                                                                    │      rename_store(0034)·rotate_invite_code(0042)
                                                                    │ 트리거: handle_new_user(0030 infallible)
                                                                    │ 테이블: profiles·units·join_attempts·
                                                                    │        unit_subscriptions
                                                                    │ RLS: 테넌트 격리(유일 방어선)
                                                                    │ realtime publication: profiles(0047)
                                                                    └───────────────────────────┘
   [엣지] functions/push  audience='join_owners' → pending_unit_id 사장에게만(양면거울✓)
```

## 규칙 복제 표 (SSOT 점검, §5-E grep)

| 규칙 | 정의처(SSOT) | 참조처 | 판정 |
|---|---|---|---|
| 전화번호 정규화 | `lib/utils/validation.ts:normalizePhone` (+DB `normalize_phone` 0022 parity 주석) | signup·account-edit·qa-onboarding(테스트 복제) | ✅ 단일 |
| emailTaken 판정 | `useSessionStore.signUp` (already/registered/exists + identities=[]) | (단일) | ✅ 단일 |
| already_in_store / already_pending / invalid_code / too_many_attempts | RPC 본문(0038·0040) 단일 | 스토어는 error.message 매칭만 | ✅ 단일 |
| join 푸시 대상 해석 | 엣지 `push/index.ts` (join_owners→pending_unit_id) | (단일) | ✅ 단일 |
| **데이터 접근 계층** | **db.ts 단일(AGENTS.md §계층경계)** | 사장측=db.ts ✅ / 본인측=db.ts 세션 데이터함수 ✅(Finding A 수렴) | ✅ **수렴됨** |
| 프로필/유닛 읽기 오류 처리 | db.ts 세션 read + loadProfile error 표면화 | loadProfile 이 `error` 포착→무음강등 차단 ✅ | ✅ **수정됨(B)** |

## RPC 정본(최고 번호 = 진실) — 라이브 검증됨(qa 19/19)

| 함수 | 정본 | 흩어진 재정의 | 라이브 상태 |
|---|---|---|---|
| create_store | **0040** (#variable_conflict use_column + WHERE NOT EXISTS) | 0002·0003·0023·0028·0036·0038·0040 | ✅ no 42702 |
| join_by_invite | **0038** (승인게이트+throttle, u./p. 한정) | 0002·0005·0009·0029·0031·0032·0038 | ✅ no 42702 |
| approve/reject/cancel | **0038** | 0032·0038 | ✅ 승인→소속확정 |
| handle_new_user | **0030** (infallible, phone충돌만 흡수) | 0001·0010·0022·0030 | ✅ 중복폰 500 없음 |
| phone_in_use / delete_my_account | **0044** (소프트삭제 제외/phone 해제) | 0022·0044 / 0005·0035·0044 | ✅ parity·재가입 |
| rename_store / rotate_invite_code | 0034 / **0042** | — / 0009·0042 | ✅ |

## 발견(2건) — 둘 다 수정 완료. 상세는 `불변식_가입합류.md`
- **A. 계층 경계 반쪽 적용**(§3③·§4.4) → ✅ **수정 완료(2026-07-06, 핫존 db.ts 커밋 후 착수)**: 본인측 데이터 접근(create_store·join·phone·cancel·leave·delete·rename RPC + profiles/units update + loadProfile 3 read)을 db.ts 세션 데이터함수(`fetchSessionProfile`·`fetchUnitInfo`·`fetchUnitSubscription`·`checkPhoneInUse`·`rpcCreateStore`·`rpcJoinByInvite`·`rpcCancelJoinRequest`·`rpcLeaveStore`·`rpcDeleteMyAccount`·`rpcRenameStore`·`updateProfileFields`·`updateUnitIndustry`)로 이전. `{data,error}` 원형 보존으로 named-에러 분기·error 시맨틱 1:1 유지. 스토어 잔여 supabase=`auth.*` 12개(세션관리)뿐. tsc·eslint·qa 19/19 green.
- **B. loadProfile 읽기 실패 위장**(§4.8·§4.10) → ✅ **수정 완료**: profiles read `error` 포착 → 기존세션 보존/콜드로드 clean signed_out + reportError. 사장→직원 무음 강등·대기직원 pending 유실 차단.
