# 🧠 MEGA-PROMPT — 알림 시스템 아키텍처 근본 수정 (SquareTable)

> 목적: "증상 하나씩 땜질 → 아키텍처 손상 + 규칙이 여러 곳에 흩어져 서로 충돌"의 악순환을 끊는다.
> 이 프롬프트는 **부분 패치를 금지**하고, 규칙을 **단일 진실원천(SSOT)**으로 수렴시킨 뒤,
> **실 백엔드에서 재현·증명**하는 것까지 강제한다. 그대로 복사해 에이전트/세션에 투입.

---

## 0. ROLE

너는 이 저장소를 책임지는 **Staff Engineer**다. 네 임무는 "알림"이 관여하는 모든 경로
(인앱 벨/목록 + OS 웹푸시 + 실시간 동기화 + RLS 쓰기)를 **하나의 일관된 아키텍처**로 정렬하고,
현재 흩어져 충돌하는 규칙을 SSOT로 통합하는 것이다. 코드를 고치기 **전에** 구조를 먼저 확정한다.

**절대 원칙**
- ❌ 증상 단위 수정 금지. ✅ 근본 원인(계층·규칙 불일치)까지 내려가서 고친다.
- ❌ 같은 규칙을 두 곳에 복제 금지. ✅ 규칙은 한 곳(SSOT)에 정의하고 나머지는 참조.
- ❌ "나중에" 금지. 지금 엣지케이스까지 명시.
- ✅ 모든 주장은 **실 백엔드 재현으로 증명**(데모/목업으로 "됐다" 판정 금지).

---

## 1. MISSION CONTEXT — 알림은 "두 평면"이 반드시 거울처럼 일치해야 한다

1. **인앱 평면**: 우상단 벨 배지(숫자) + 알림 목록. SSOT = `src/lib/utils/notifications.ts`
   (`buildJuniorNotifications`/`buildOwnerNotifications`, `*UnreadCount`).
2. **OS 푸시 평면**: 앱이 꺼져 있어도 오는 웹푸시. 발송 = `src/lib/push/notify.ts` → `supabase/functions/push`.

**불변식(INV-MIRROR): 한 이벤트는 두 평면에서 동일한 대상·동일한 의미를 가져야 한다.**
- 예: "직원 언급"은 인앱 벨에도 뜨고 OS 푸시로도 가야 하며, 둘의 수신자·클릭목적지가 일치해야 한다.
- 현재 이 거울이 깨진 지점을 전수 조사해 목록화하고 전부 정합화하라.

---

## 2. 확인된 근본 원인 카탈로그 (재발견 말고 **검증**부터)

아래는 이미 실 백엔드에서 확인된 뿌리다. 각 항목을 **재현→수정→재현(통과)** 사이클로 처리하라.

| ID | 근본 원인 | 증상 | 검증된 사실 |
|----|-----------|------|-------------|
| RC1 | 직원이 `upsertFeed`(upsert)로 남(사장)의 공지 행을 건드림 → upsert의 INSERT 경로가 `wf_insert`(notice=사장전용)에 걸림 | "읽음 표시 저장 실패" 오류 + 읽음 영구 미저장 + 안읽음 배지 안 지워짐 | 라이브: junior `upsert`=**42501**, `update`=**성공** |
| RC2 | 안읽음 모델 파편화 — `juniorUnreadCount`=미읽음공지+받은교대만. 멘션/교대결과는 `unread:false`라 미집계. 사장엔 멘션 자체가 없었음 | 배지가 "다른 앱처럼" 안 셈, 일부 알림이 숫자에 안 잡힘 | 코드 확인 |
| RC3 | `profiles`가 `supabase_realtime` publication에 **미등록** → `subscribeStaff`(profiles 구독) 콜백이 영영 안 울림 | 합류 신청이 사장 화면에 실시간 반영 안 됨(재시작해야 보임) | migration 전수: profiles add 없음 |
| RC4 | 이중경로 규칙 불일치 — 발송(notify.ts)은 수신자 역할을 모른 채 `/junior/*` 고정 → 사장이 받으면 클릭 경로 오라우팅 | 사장 멘션 알림 클릭 시 엉뚱한 화면 | 코드 확인 |
| RC5 | 발송 트리거 규칙이 화면/스토어에 흩어짐 — 일반 채팅=푸시 없음(설계), 공지/멘션만 발송. 이 규칙이 문서화·중앙화 안 됨 | "채팅 썼는데 알림 안 와" 혼란 | useWorkStore postMessage 확인 |
| RC6(플랫폼) | iOS PWA 백그라운드에서 realtime 소켓 끊김 + iOS 웹푸시 지연 | 인앱 알림 ~2분 지연 | 플랫폼 특성(코드버그 아님) → 문서화+UX 고지 |

> ⚠️ 이 표를 **맹신하지 말 것.** 각 RC는 "현재도 그러한지" 라이브로 먼저 재확인한 뒤 착수한다
> (스키마·정책은 대시보드 수동적용이라 원장과 실제가 어긋날 수 있음 — AGENTS.md 참조).

---

## 3. 반드시 성립시켜야 할 불변식(INVARIANTS) — 이게 SSOT다

작업 전, 아래 불변식을 `docs/알림_불변식.md`(신규)에 표로 확정하고, 모든 코드가 이를 참조하게 하라.

- **INV-MIRROR**: 인앱 평면과 OS 푸시 평면은 이벤트·수신자·클릭목적지가 1:1.
- **INV-WRITE-RLS**: "남이 만든 행"의 제자리 수정은 **UPDATE**만 사용(upsert 금지). notice INSERT는 사장만.
  테넌트 격리는 RLS USING이 유지(같은 매장만). (기준: `wf_update` vs `wf_insert`.)
- **INV-UNREAD**: 안읽음 배지 = 사용자가 "알림함을 마지막으로 본 시각(`notifsSeenAt`)" 이후 발생한 알림 수.
  종류(공지·멘션·교대·결과·합류신청) **불문 통합 집계**. 알림함 진입 시 `notifsSeenAt=now` → 배지 0.
  (배지는 개별 read_by 쓰기에 **의존하지 않는다** → RLS/경쟁조건과 무관하게 항상 정확.)
- **INV-READFLAG**: 개별 공지 "굵게(미열람)" 표시는 read_by 유지하되 쓰기는 UPDATE로. 실패해도 배지는 무영향.
- **INV-REALTIME**: 클라가 `subscribeX`로 구독하는 모든 테이블은 반드시 publication 멤버여야 한다.
  (구독 코드와 publication 목록의 **차집합 = 0** 을 CI/점검 스크립트로 강제.)
- **INV-SENDER**: 발송자 본인에게는 어떤 평면으로도 알림이 가지 않는다(메아리 금지).
- **INV-TENANT**: 어떤 알림도 발송자/수신자의 매장(unit) 경계를 넘지 않는다(푸시 함수·RLS 이중).
- **INV-TRIGGER-SSOT**: "어떤 사용자 행동이 어떤 알림을 만드는가"의 매핑을 **한 표**로 정의하고,
  스토어/화면은 그 표의 헬퍼만 호출한다(문자열·조건 중복 금지).

---

## 4. METHODOLOGY — 순서를 지켜라(코드는 4d에서 처음 씀)

**4a. MAP (현 구조 전수 조사)**
- 알림 관련 전 파일을 그래프로: 트리거(스토어 write) → 인앱 빌더 → 벨 배지 → OS 발송(notify) → 엣지함수 → SW → 클릭 라우팅 → realtime 구독 → RLS 정책.
- "같은 규칙이 몇 곳에 복제됐는지"를 표로(예: 수신자 판정, 읽음 판정, 경로 문자열).

**4b. DEFINE (SSOT 확정)**
- §3 불변식을 문서화. 규칙이 흩어진 곳을 SSOT 참조로 바꿀 계획을 명시(무엇을 어디로 흡수하는가).

**4c. VERIFY-BEFORE (라이브 재현)**
- `.env.seed`(service_role) + `.env`(anon)로 **실 백엔드에 매직링크 세션**을 만들어 각 RC를 재현.
  (기법 검증됨: `admin.auth.admin.generateLink({type:'magiclink'})` → `verifyOtp` → 그 세션으로 RLS 실측.)
- 재현 스크립트는 `scripts/_diag-*.mjs`로 만들고 **실행 후 즉시 삭제**(레포 오염 금지, service_role 노출 주의).

**4d. FIX-AT-ROOT (최소 surgical + 규칙 통합)**
- 각 수정은 불변식 1개 이상에 연결. 규칙은 SSOT로 흡수, 호출부는 참조만.
- ⚠️ **핫존 주의**: `useWorkStore.ts`·`db.ts` 등에 다른 미커밋 변경 존재. 함수 단위로만 손대고,
  건드린 파일·함수를 사용자에게 명시. `git status`+mtime로 핫존 재확인 후 진입.

**4e. VERIFY-AFTER (증명)**
- 각 RC를 4c와 동일 스크립트로 재실행 → 통과 증거를 남긴다.
- 회귀 하니스 추가: `scripts/qa-notifications.mjs`(실 백엔드·자가정리) —
  ①직원 읽음표시 update 성공 ②타매장 직원 update 차단 ③배지 seenAt 집계 ④profiles realtime 구독 이벤트 수신
  ⑤발송자 제외 ⑥크로스테넌트 발송 거부. `package.json`에 `qa:notifs` 등록, CI(qa-onboarding.yml 패턴)로 감시.

---

## 5. CONSTRAINTS (위반 시 자동 실패)

- **DB 적용**: `db push` 맹목 금지. 마이그레이션은 **대시보드 SQL 수동적용**이 기본. 원장(0041~) 미reconcile —
  새 마이그레이션은 다음 빈 번호 사용, 단일 SQL로 넘겨 사용자가 적용(멱등 `do $$…$$` 가드).
- **RLS**: 성능≠보안 분리. USING/WITH CHECK 의미 보존 기본. 격리 완화는 별도 커밋 + `/cso` + 크로스테넌트 `/qa`.
- **가입/합류 RPC** 건드리면 `npm run qa:onboarding` green 필수. 함수 본문은 `pg_get_functiondef`로 직접 확인.
- **프레임 불변식**: 웹에서도 460px 모바일 프레임 이탈 금지(Modal/시트 `modalFrameStyle`).
- **토큰 규칙**: 간격·라운드·그림자·거터는 `Space/Radius/Elevation/SCREEN_GUTTER` 토큰만. 로컬 매직넘버 금지.
- **시장**: KST 단일. 타임존 분기 만들지 말 것.
- **모델**: LLM 관련이면 최신 Claude(Opus 4.8 등) 기준.

---

## 6. ANTI-PATTERNS (이번에 실제로 사고 낸 것들 — 재발 금지)

1. 남이 만든 행을 `upsert`로 수정(→ INSERT RLS 42501). **제자리 UPDATE 써라.**
2. 같은 판정(수신자·읽음·경로)을 화면마다 복붙 → 한 곳만 고쳐지고 나머지 drift. **SSOT 참조.**
3. `subscribeX`는 있는데 그 테이블이 publication에 없음 → "실시간"이 조용히 죽음. **차집합 0 강제.**
4. 발송 측이 수신자 역할을 가정(`/junior/*` 고정) → 사장 오라우팅. **수신 시점 역할로 해석.**
5. 데모/목업으로 "동작 확인" → 스키마·RLS 드리프트를 못 잡음. **실 세션 재현으로만 판정.**
6. 배지를 개별 read 쓰기에 의존 → RLS/경쟁조건에 취약. **seenAt 파생으로 분리.**

---

## 7. DELIVERABLES

1. `docs/알림_아키텍처.md` — 4a 구조 그래프 + 규칙 복제 지점 표.
2. `docs/알림_불변식.md` — §3 불변식 SSOT 표.
3. 근본 수정 코드 (RC1~RC5) — 각 커밋이 불변식에 매핑, 핫존 파일 명시.
4. `supabase/migrations/00XX_*.sql` (멱등) + 대시보드 적용용 단일 SQL 블록.
5. `scripts/qa-notifications.mjs` + `qa:notifs` + CI 배선.
6. 라이브 재현 **전/후 로그**(각 RC PASS 증거).
7. 변경 요약: 무엇을 SSOT로 흡수했고, 어떤 중복을 제거했는지.

---

## 8. DEFINITION OF DONE (Go/No-Go)

- [ ] 4a 구조도 + 규칙 복제 표 존재
- [ ] §3 불변식 전부 코드에 반영, 규칙 중복 제거(참조화)
- [ ] RC1~RC5 각각 라이브 재현 **전(FAIL)→후(PASS)** 로그
- [ ] `subscribe`↔publication 차집합 = 0 (스크립트 증명)
- [ ] 인앱↔OS푸시 거울 일치(이벤트·수신자·경로) 표로 증명
- [ ] `qa:notifs`·`qa:onboarding` green, `typecheck` 통과
- [ ] 마이그레이션은 단일 SQL로 분리, 핫존 파일 사용자 고지
- [ ] 신규 의존성 0 (또는 정당화)

**끝맺음 규칙**: 하나라도 미충족이면 "부분 완료"가 아니라 **미완료**다. 증상이 사라진 것과 뿌리가
고쳐진 것은 다르다 — 반드시 라이브 재현으로 뿌리를 증명하라.
