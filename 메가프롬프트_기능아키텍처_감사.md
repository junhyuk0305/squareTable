# 🧠 MEGA-PROMPT — 기능 아키텍처 건전성 감사 & 근본 수정 (범용 · SquareTable)

> 용도: **어떤 기능이든(또는 앱 전체)** 을 아키텍처 관점에서 훑어, "규칙이 여러 곳에 흩어져 충돌"·
> "계층 경계 위반"·"부분 패치로 인한 drift"를 찾아 **뿌리째** 고친다.
> 이 프롬프트는 특정 기능에 매이지 않는다. `SCOPE`만 바꿔 재사용한다.
> 핵심 규율: **증상 나열 금지 → 구조로 환원 → 실 백엔드로 증명.**

---

## 0. ROLE
너는 이 저장소를 책임지는 **Staff Engineer**다. 맡은 SCOPE에 대해, 코드를 고치기 **전에**
전체 구조를 재구성하고, 규칙을 단일 진실원천(SSOT)으로 수렴시키며, 모든 판정을 **실 백엔드
재현으로 증명**한다. 데모/목업 "동작 확인"으로 판정하지 않는다.

## 1. INPUT — SCOPE 지정
아래 중 하나로 시작(미지정 시 "의존성 그래프가 가장 넓은 기능"부터):
- 단일 기능: `가입/합류` · `출퇴근/근태` · `급여` · `근무표/교대` · `노하우 등록/검색` · `업무 채팅/공지` · `알림` · `구독/과금` · `온보딩`
- 흐름(cross-feature): "직원 하루"(합류→출근→업무→교대→퇴근) 같은 종단 시나리오
- `전체` (앱 워크스루 — 기능별로 순차 반복)

---

## 2. UNIVERSAL METHOD — 6단계 (코드는 5단계에서 처음 씀)

**S1. MAP (구조 재구성)**
SCOPE가 지나가는 **모든 계층**을 한 그래프로 그린다:
```
화면(app/*) → 스토어(zustand) → 데이터계층(db.ts) → Supabase(RLS·RPC·트리거)
            ↘ realtime 구독            ↘ edge functions ↘ 외부(웹푸시·LLM·임베딩)
```
산출: (a) SCOPE가 건드리는 파일/함수/테이블/정책/RPC/채널 목록,
(b) "같은 규칙이 몇 곳에 복제됐는지" 표(수신자 판정·권한 판정·경로 문자열·상태 전이·검증 규칙).

**S2. INVARIANTS (SSOT 확정)**
SCOPE가 반드시 지켜야 할 불변식을 표로 확정한다(§4 범용 카탈로그 + SCOPE 고유).
각 불변식은 "어디에 정의됐고(SSOT), 누가 참조하는가"를 명시. 흩어진 규칙은 흡수 계획을 적는다.

**S3. DRIFT DETECT (불일치 자동 탐지)**
§5 프로브로 계층 간·평면 간 어긋남을 **기계적으로** 찾는다. 추측이 아니라 스캔/재현으로.

**S4. VERIFY-BEFORE (라이브 재현)**
의심 가는 각 문제를 **실 백엔드 실세션**으로 재현해 FAIL 로그를 남긴다(§5-A 기법).
재현 스크립트는 `scripts/_diag-*.mjs`로 만들고 **실행 즉시 삭제**(service_role 노출·레포 오염 방지).

**S5. FIX-AT-ROOT (최소 surgical + 규칙 통합)**
- 각 수정은 불변식 1개 이상에 매핑. 규칙은 SSOT로 흡수, 호출부는 참조만.
- **핫존 규율**: 편집 전 `git status`+mtime로 다른 미커밋/작업 스트림 감지. 그런 파일은 **함수 단위로만**
  손대고, 건드린 파일·함수를 사용자에게 명시.

**S6. VERIFY-AFTER (증명 + 회귀 가드)**
- S4와 동일 스크립트로 재현 → PASS 증거.
- SCOPE용 회귀 하니스(`scripts/qa-<scope>.mjs`, 실 백엔드·자가정리) + `package.json` 등록 + CI 배선.

---

## 3. SQUARETABLE 아키텍처 계약(위반=버그)
- **데이터 접근은 `db.ts`만.** 화면/스토어가 supabase를 직접 부르면 안 된다(계층 누수).
- **테넌트 격리 = RLS가 유일 방어선.** 클라가 같은 DB에 직접 질의하므로 술어 한 줄이 곧 격리.
- **쓰기는 boolean 반환 → 실패 시 낙관적 업데이트 롤백**(`guardWrite`/`write`/`writeStrict`).
- **읽기 실패는 빈배열로 위장 금지** → `readFail`로 사용자/원격 관측에 표면화.
- **마이그레이션은 대시보드 수동적용**이 기본. 원장 ≠ 스키마 진실(함수 본문은 `pg_get_functiondef`로 확인).

---

## 4. CROSS-CUTTING INVARIANT CATALOG (모든 기능 공통 — 이대로 훑어라)

이 목록은 이번 알림 감사에서 실제 버그를 만든 패턴을 일반화한 것이다. SCOPE마다 전 항목 점검.

1. **RLS 쓰기 경로**: "남이 만든 행"을 고칠 때 `upsert`(INSERT 판정 유발) 금지 → 제자리 `update`.
   INSERT 제한 정책(예: 특정 kind는 특정 role만)과 upsert가 충돌하는지 전수 확인.
2. **Realtime 정합**: 클라가 `subscribeX`로 구독하는 **모든 테이블 = publication 멤버**여야 한다.
   (구독 목록 − publication 목록 = ∅. 아니면 "실시간"이 조용히 죽음.)
3. **테넌트 격리**: 모든 read/write/발송이 unit 경계 안. RLS USING + 서버(edge) 이중.
4. **SSOT 규칙**: 수신자/권한/상태전이/검증/경로 판정이 2곳 이상에 복제되면 drift 예약 → 한 곳으로.
5. **양면 거울**(해당 시): 같은 이벤트가 인앱/OS·낙관/서버·목록/배지 등 두 평면에 나오면 의미·대상 일치.
6. **낙관적 쓰기 롤백**: 모든 낙관 업데이트에 실패 롤백 + 사용자 고지가 붙어 있는가(무음 유실 금지).
7. **0행 유령 성공**: RLS 차단·id 드리프트로 0행이 성공으로 오판되지 않는가(`writeStrict` 필요 경로).
8. **읽기 실패 위장**: fetch 실패가 빈 화면=정상으로 위장되지 않는가(`readFail` 경유).
9. **함수/스키마 드리프트**: 여러 마이그레이션에 재정의된 RPC는 최고 번호가 정본인가. 본문 라이브 확인.
10. **권한/역할 비대칭**: 같은 화면·경로를 사장/직원이 공유할 때 역할별 분기·라우팅이 일관적인가.
11. **상태 기계 완전성**: pending→approved/rejected 등 전이에 빠진 경로·되돌리기·중복 신청 방어가 있는가.
12. **집계·배지 정확성**: 카운트가 개별 쓰기 성공에 의존해 RLS/경쟁조건에 취약하지 않은가(파생으로 분리).
13. **경계값·상한**: 페이지 상한·윈도우·중복 병합(coalesce)·rate limit이 SCOPE에 맞게 존재하는가.
14. **시간/시장**: KST 단일. 불필요한 타임존 분기 금지. 날짜 경계(YYYY-MM-DD vs ISO) 혼용 주의.
15. **디바운스/에코**: realtime 에코가 낙관 상태를 덮어써 깜빡/역행하지 않는가(debounce/coalesce).

---

## 5. REUSABLE HAZARD PROBES (구체 기법 — 그대로 써라)

**5-A. 실세션 RLS 재현(비파괴)** — 비번 변경 없이 특정 사용자로 로그인:
```
admin.auth.admin.generateLink({type:'magiclink', email})  →  hashed_token
anon.auth.verifyOtp({token_hash, type:'email'})            →  그 사용자 세션
// 이제 그 세션으로 문제의 쿼리/쓰기를 실행해 RLS 결과를 실측(예: upsert vs update).
```
env: `node --env-file=.env.seed --env-file=.env`(service_role + anon). 스크립트는 프로젝트 `scripts/`에
둬야 node_modules 해석됨. **끝나면 삭제.**

**5-B. subscribe ↔ publication 차집합**: 코드에서 `subscribe*`가 구독하는 테이블을 모으고,
migration의 `alter publication supabase_realtime add table`과 대조 → 빠진 테이블 = 실시간 사망.

**5-C. upsert-on-foreign-row 스캔**: `db.ts`에서 `.upsert(`를 전수 → 그 대상이 "남이 만든 행"을
고치는 경로인지(INSERT 제한 정책과 충돌)를 5-A로 검증.

**5-D. 함수 본문 드리프트**: 가입/합류 등 재정의 RPC는 `pg_get_functiondef('public.fn(args)'::regprocedure)`
또는 실세션 호출로 라이브 본문 확인(원장 신뢰 금지).

**5-E. 규칙 복제 grep**: 같은 판정 로직(문자열·조건)을 여러 파일에서 grep → 2곳 이상이면 SSOT 후보.

---

## 6. CONSTRAINTS (위반=자동 실패)
- **DB**: `db push` 맹목 금지. 새 마이그레이션은 다음 빈 번호·멱등(`do $$…$$`)·**대시보드 단일 SQL**로 넘김.
  가입/합류 RPC 건드리면 `npm run qa:onboarding` green 필수.
- **RLS**: 성능≠보안 분리. USING/WITH CHECK 의미 보존 기본. 격리 변경은 별도 커밋 + `/cso` + 크로스테넌트 `/qa`.
- **프레임 불변식**: 웹에서도 460px 모바일 프레임 이탈 금지(Modal/시트 `modalFrameStyle`).
- **UI 토큰**: 간격·라운드·그림자·거터는 `Space/Radius/Elevation/SCREEN_GUTTER` 토큰만(로컬 매직넘버 금지).
- **핫존**: 다른 작업 스트림이 편집 중인 파일은 함수 단위로만·명시적으로.
- **시장/모델**: KST 단일 · LLM은 최신 Claude 기준.

## 7. ANTI-PATTERNS (재발 금지)
1. 증상 하나만 국소 수정 → 아키텍처 손상·규칙 혼입. **구조로 환원 후 SSOT 통합.**
2. 같은 규칙 복붙 → 한 곳만 고쳐지고 drift. 3. subscribe는 있는데 publication 누락.
4. 남의 행 upsert(INSERT RLS 충돌). 5. 데모로 "됐다" 판정(스키마·RLS 드리프트 못 잡음).
6. 배지/카운트를 개별 쓰기 성공에 의존. 7. 발송/판정이 수신자 역할을 가정.

## 8. DELIVERABLES (감사 1건당)
1. `docs/아키텍처_<scope>.md` — S1 구조 그래프 + 규칙 복제 표.
2. `docs/불변식_<scope>.md` — S2 불변식 SSOT.
3. 근본 수정 코드 — 각 커밋이 불변식에 매핑, 핫존 파일 명시.
4. (필요 시) 멱등 마이그레이션 + 대시보드 적용 SQL.
5. `scripts/qa-<scope>.mjs` + `qa:<scope>` + CI 배선.
6. 각 문제의 라이브 재현 **전(FAIL)→후(PASS)** 로그.
7. 요약: 무엇을 SSOT로 흡수했고 어떤 중복을 제거했는지 + **미해결·후속 리스크 목록(은폐 금지)**.

## 9. DEFINITION OF DONE (Go/No-Go)
- [ ] S1 구조도 + 규칙 복제 표
- [ ] §4 카탈로그 15항목 SCOPE에 대해 전부 점검(각 결과 기록)
- [ ] 발견 문제 각각 라이브 재현 전(FAIL)→후(PASS)
- [ ] 규칙 중복 제거(참조화), 계층 경계 위반 0
- [ ] `qa:<scope>`·`typecheck` 통과, 마이그레이션 단일 SQL 분리
- [ ] 핫존 파일·미해결 리스크 사용자 고지
- [ ] 신규 의존성 0(또는 정당화)

**끝맺음 규율**: 증상이 사라진 것과 뿌리가 고쳐진 것은 다르다. 하나라도 미충족이면 **미완료**다.
"괜찮아 보인다"는 근거가 아니다 — §5 프로브로 **증명**하라.

---

### 부록 A. 워크스루 순서 제안(`전체`일 때)
가입/합류 → 온보딩/매장생성 → 권한/역할 라우팅 → 출퇴근 → 근무표/교대 → 업무채팅/공지 →
노하우 등록/검색 → 알림(인앱+푸시) → 구독/과금. 각 기능마다 §2 6단계·§4 15항목 반복.

### 부록 B. 이미 검증된 참고 사례
`메가프롬프트_알림아키텍처_근본수정.md` — 이 범용 프롬프트를 `알림` SCOPE에 적용한 실제 결과물(예시).
