import { StyleSheet } from 'react-native';
import { InkColors } from '@/lib/theme/colors';

/** 노하우 탭(주니어) 라우트 셸 공용 스타일. JuniorChatScreen이 소유. */
export const styles = StyleSheet.create({
  safe: { flex: 1, backgroundColor: InkColors.cream },
});
