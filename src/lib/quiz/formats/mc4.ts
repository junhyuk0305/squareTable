// t0 기본 — 상황 한 줄 + 4지선다. 07-29 §03 T0.
// 유형 판정에 실패한 노하우(상황만 있고 나머지가 빈 것)의 안전망이다.
// 이게 없으면 내용이 부실한 노하우에서 문항이 통째로 안 만들어진다.
// 기존 엣지 task:'quiz' 가 만들던 모양과 같다(하위호환).

import { choicePickSpec } from './spec';

export const mc4 = choicePickSpec({
  key: 'mc4',
  kind: 't0',
  label: '4지선다',
  seconds: 15,
  aiHint:
    '현장에서 실제로 마주칠 상황 한 줄(ask)과 대응 선택지 3~4개를 만들어라. '
    + '정답은 노하우에 근거가 분명한 것 하나만 두고, 오답은 그럴듯하되 노하우와 명백히 어긋나게 만들어라.',
});
