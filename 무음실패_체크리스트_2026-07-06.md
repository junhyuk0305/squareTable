# 스퀘어테이블 무음 실패 체크리스트 (2026-07-06)

> **무음 실패** = 실패했는데 사용자가 신호를 못 받거나, 오히려 성공 신호를 받는 것.
> 근거: `src/lib/db.ts` 전문 + 9개 스토어 + 화면 10곳 코드 실검증.
> 기존 문서 `신뢰성_계측_감사_2026-07-02.md`의 구조 공백(P0-1 읽기실패 위장 / P1-6 0행 유령성공)을 **기능·위치 단위로 구체화**한 실행용 체크리스트.

---

## 🟥 A. 읽기 실패가 "빈 화면"으로 위장 (구조적·전면적) — P0

원리: 모든 `fetch*`는 실패 시 `[]`/`{}`/`null` 반환 + **모든 스토어 `hydrate()`가 실패해도 `loaded=true`** → 백엔드 장애가 "데이터 없음"과 화면상 동일. 유일 신호는 전역 배너(B의 한계 있음).

- [ ] 받은질문 인박스 — `owner/inbox.tsx` 실패 시 "아직 새 질문이 없어요"로 위장, 사장이 질문 방치
- [ ] 내 노하우 — `owner/knowledge.tsx`·`owner/categories.tsx` "등록된 노하우가 없어요" (fetchEntries 실패 ≠ 신규 매장)
- [ ] 사장 대시보드 KPI — `owner/dashboard.tsx` 근무중·인건비·업무완료 전부 0, entriesCount===0을 신규 신호로도 사용
- [ ] 직원 명부/합류 신청 — fetchStaffProfiles/fetchPendingMembers 실패 시 0명·0건 → "초대했는데 안 들어오네" 오해
- [ ] 근무표 — owner/junior schedule 시프트·교대요청 로드 실패가 빈 근무표로
- [ ] 알림함 — junior/owner notifications "새 알림이 없어요"로 조용히 빔
- [ ] 노하우 둘러보기 — junior/chat 둘러보기 빈 카드
- [ ] 급여(별종) — usePayrollStore는 `loaded` 플래그 자체가 없음 → 실패 여부조차 알 수 없음

**대응:** 스토어에 `loadError` 상태 → 화면에서 EmptyState 대신 "불러오지 못했어요 · 다시 시도" 재시도 UI (인박스·노하우·명부 우선).

---

## 🟥 B. 유일한 실패 신호인 배너 자체가 취약 — P1

`SyncBanner.tsx` + `useSyncStore.ts`:

- [ ] 읽기 실패 ≠ 쓰기 실패 구분 없음 (`error: string|null` 단일 필드, 같은 아이콘·배너)
- [ ] 덮어쓰기 유실 — `noteError`가 단순 set → 동시 실패 시 나중 것만 남음
- [ ] 탭 한 번이면 영구 소멸 — 닫기 X만 clear, 재노출 경로 없음 (A와 결합 시 장애=빈 화면)
- [ ] 자동 정리 안 됨 — 이후 성공해도 stale 배너 잔존

---

## 🟥 C. 쓰기 0행 유령 성공 (writeStrict 미적용 무결성 경로) — P0~P1

원리: `write()`(db.ts:29)는 `error=null`만 성공으로 봄 → RLS차단·id드리프트·동시삭제로 0행 UPDATE/DELETE/UPSERT가 성공(true) 오판 → 롤백·배너 없이 유실. `writeStrict`는 3곳(updateUnknownStatus·resolveUnknown·upsertAttendance)에만 적용.

- [ ] **P0 · 교대 승인/수락** — `updateSwap`(db.ts:886) plain. 0행이어도 "승인됨" + `notifyUserSwapResult(...,true)` 푸시 발송, DB는 accepted 잔존. 최악의 불일치
- [ ] **P0 · 출퇴근 삭제** — `deleteAttendance`(db.ts:681) plain. upsert만 writeStrict, delete 누락 → 급여 총액 어긋남
- [ ] **P1 · 시급 저장** — `setWageDb`(db.ts:698) UPSERT plain
- [ ] **P1 · 제안 승인/반려** — `reviewSuggestion`(db.ts:266) UPDATE plain
- [ ] **P1 · 노하우 수정/삭제** — `updateEntry`/`deleteEntry`(db.ts:229/240) plain
- [ ] **P1 · 주간 근무표 저장** — `saveStaffShifts`(db.ts:819) delete+upsert plain
- [ ] **P2 · 업무보드** — setDone/clearDone/deleteFeed/updateTemplate/deleteTemplate/upsertFeed
- [ ] **P2 · 기타** — updateShiftTemplate·deleteShiftTemplate·upsertScheduleConfig·bumpUnknownSimilar·updateChatSatisfaction

**대응:** 무결성 경로를 writeStrict로 승격. 단 "쓰기 후 SELECT 가능(RLS SELECT 허용)" 경로만.

---

## 🟧 D. 성공 UI가 서버 결과와 분리 (낙관적 토스트/네비) — P1~P2

`add`/`guardWrite`가 `Promise<boolean>` 반환하나 화면이 await·검사 없이 성공 토스트/전환 즉시 실행.

- [ ] **P1 · 알바 노하우 제안** — junior/suggest.tsx:65-72 `submit` 후 즉시 "제안을 보냈어요" + back
- [ ] **P1 · 온보딩 노하우 등록** — owner/onboarding.tsx:96-99 `addEntry` fire-and-forget 후 setStep('done')
- [ ] **P1 · 템플릿 담기** — TemplateLibrary.tsx:204-213 `add` 반환 무시하고 성공 다이얼로그
- [ ] **P2 · 가게 기본정보 저장** — store-config.tsx:36-38 무조건 "저장됐어요 ✓" + back
- [ ] **P2 · 채팅방 생성/삭제** — rooms.tsx:41,163 토스트가 서버 확인 전

**대응:** `if (await submit(...)) 성공 else 실패·화면유지`. `account-edit.tsx`가 모범.

---

## 🟧 E. 백엔드 no-op인데 성공처럼 보임 — P1

- [ ] **입금 완료 버튼** — billing.tsx:58-61 `notifyPaid`가 백엔드로 아무것도 안 보냄(파이프라인 미구현). 결제/전환 직결 무음
- [ ] **급여 설정** — usePayrollStore.ts:42 `setSetting`이 DB 저장 전혀 없음. 휴게공제·야간/연장/주휴·추가수당·정산일·급여일 리로드 시 소실. setWage는 저장되는데 settings만 누락

---

## 🟨 F. 실패를 삼키고 무피드백 — P2

- [ ] **초대코드 재발급** — staff.tsx:84-89 `rotateInviteCode` null(실패) 시 모달만 닫고 무메시지
- [ ] **직원 내보내기 원격관측 누락** — removeStaffMember(db.ts:137)만 write() 헬퍼 미사용 → reportError 없음

---

## 우선순위 Top 5

| # | 항목 | 카테고리 | 이유 |
|---|------|---------|------|
| 1 | 읽기 실패 → 재시도 UI (인박스·노하우·명부) | A | 장애가 빈 화면 위장 |
| 2 | updateSwap writeStrict 승격 | C | 근무 배정 뒤틀림 + 거짓 푸시 |
| 3 | deleteAttendance writeStrict 승격 | C | 급여 총액 직결 |
| 4 | 급여 설정 영속화 | E | 설정 조용히 소실 |
| 5 | 알바 제안 성공 게이팅 | D | 제안 유실 = 노하우 파이프라인 끊김 |

---

## ✅ 적용 현황 (2026-07-06, tsc+lint 통과·미배포)

### 이번에 수정 완료
- **C (0행 유령 성공)** — `updateSwap`·`deleteAttendance`·`setWageDb`·`reviewSuggestion`·`updateEntry`·`deleteEntry` 6경로를 `writeStrict`(`.select()` 영향행 확인)로 승격. 모두 단일행(id/pk)이라 오탐 없음.
- **D (성공 UI 게이팅)** — 알바 제안(`junior/suggest`, store `submit`이 boolean 반환하도록)·온보딩 노하우 등록(`owner/onboarding`, 저장 개수 정직 반영)·템플릿 담기(`TemplateLibrary`, `add` 결과 확인) 3곳을 서버 결과 확인 후에만 성공 처리하도록 변경 + 진행중 버튼 상태.
- **F (실패 무피드백)** — `rotateInviteCode` 실패 시 토스트 추가, `removeStaffMember`를 `write()` 헬퍼 경유로 원격관측 복구.
- **A (읽기 실패 위장)** — db.ts 4개 fetch가 `{data,error}` 반환 → `usePlaybookStore`·`useUnknownQueueStore`·`useStaffStore`에 `loadError` 추가 → **인박스·노하우(둘러보기)·직원 명부** 3화면이 빈 상태 대신 "다시 시도" 노출.
- **E (급여 설정 소실)** — `usePayrollStore.setSetting`을 localStorage 로컬 영속(`usePreferencesStore` 패턴)으로 전환 → 새로고침해도 유지. (설정이 급여 계산에 실제 반영되는 단계에서 매장 단위 DB로 승격 예정)

### 남은 항목 (후속)
- **B (배너 구조)** — 읽기/쓰기 실패 구분·덮어쓰기 유실·탭 시 영구소멸. 배너 시스템 리디자인 필요(단일 세션 범위 아님).
- **C-나머지** — `saveStaffShifts`(다중행 `.in()` 삭제 오탐 위험으로 보류)·업무보드/기타 P2 UPSERT/DELETE.
- **D-P2** — `store-config`·`rooms` 낙관적 토스트(스토어 액션이 void 반환 → boolean 반환으로 리팩터 필요).
- **A-확장** — 대시보드·근무표·알림함·둘러보기(직원)로 `loadError` 확대(schedule/work/attendance 스토어에도 추가).
- **E-DB** — 급여 설정 매장 단위 DB 영속(급여 계산 기능과 함께, 마이그레이션 수동 적용 필요).
- **billing `notifyPaid`** — 입금 알림 백엔드 파이프라인(엣지 함수) 미구현. 별도 작업.
</content>
</invoke>
